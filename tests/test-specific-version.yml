name: Test specific version pinning
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a clean Python file to lint
        run: |
          mkdir -p /tmp/ruff-test
          cat > /tmp/ruff-test/hello.py << 'EOF'
          x = 1
          EOF

      - id: action
        uses: .
        with:
          args: check
          src: /tmp/ruff-test
          version: "0.6.0"

      - name: Assert ruff-version output matches pinned version
        env:
          RUFF_VERSION: ${{ steps.action.outputs.ruff-version }}
        run: |
          echo "ruff-version: $RUFF_VERSION"
          if [ "$RUFF_VERSION" != "0.6.0" ]; then
            echo "FAIL: expected ruff-version '0.6.0', got '$RUFF_VERSION'"
            exit 1
          fi
          echo "PASS: ruff-version=$RUFF_VERSION"
