[project]
name = "test-project"
version = "0.1.0"
description = "Test project for ruff-action"
requires-python = ">=3.12"
dependencies = [
    "ruff==0.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
